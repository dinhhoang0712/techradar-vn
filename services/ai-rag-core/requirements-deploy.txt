# API framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
sse-starlette==2.1.3

# Database clients
neo4j==5.19.0
SQLAlchemy==2.0.35
asyncpg==0.29.0
greenlet==3.1.1

# Config & validation
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1

# LLM providers
langchain==0.2.16
langchain-core==0.2.43
langchain-google-genai==1.0.10
google-generativeai==0.7.2
langchain-openai==0.1.25
tiktoken==0.7.0

# LLM Gateway — package dùng chung nhiều service (services/llm-gateway/): fallback giữa
# provider, rate limit theo token, tính cost. generator.py import trực tiếp; [all] kéo theo
# openai/groq/google-generativeai/anthropic/redis SDK. Cài editable từ path local trong repo —
# Dockerfile COPY services/llm-gateway vào /llm-gateway để path này resolve được lúc build.
-e ../llm-gateway[all]

# Embedding, NER, and reranking
sentence-transformers==3.0.1
transformers==4.44.2
torch>=2.0.0
onnxruntime==1.20.1
huggingface-hub==0.36.0

# HTTP
httpx==0.27.2
aiohttp==3.13.5

# Logging & monitoring
python-json-logger==2.0.7
prometheus-client>=0.20.0

# Numerical (forecast signals)
numpy>=1.26.0
