# DVC sẽ tự quản lý các thư mục data/* — không commit raw vào git
data/raw/
data/features/
data/models/
data/labels/
data/metrics/

# MLflow local — file-store bake absolute path của máy hiện tại vào DB
# (artifact_location, runs.artifact_uri, model_versions.source...) nên KHÔNG
# portable giữa các máy — coi là state cục bộ theo máy, giống data/*.
mlruns/
mlruns.db

# Python
__pycache__/
*.pyc
.pytest_cache/

# DVC artefacts trung gian
.dvc/tmp/
.dvc/cache/
