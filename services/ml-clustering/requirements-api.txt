# Minimal runtime dependencies for serving the FastAPI clustering API.
# Use this when you only need to run app.main and read exported artifacts
# from local disk or S3. The full ML pipeline dependencies live in
# requirements.txt.

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
boto3==1.34.162
pandas==2.2.2
pyarrow==16.1.0
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1
PyYAML>=6.0.0
python-json-logger==2.0.7
prometheus-client>=0.20.0
loguru==0.7.2

# LLM auto-label (POST /pipeline/trigger runs stage_04_label in-process via
# src/labeling/llm_labeler.py — needed here too, not just in requirements.txt).
openai>=1.40.0,<2.0.0
google-generativeai==0.7.2
groq>=0.9.0,<1.0.0
# Read-only MlflowClient queries for GET /pipeline/runs (model quality history) — the
# API-only image never trains, but needs this to read the same tracking store stage_03
# writes to (must match requirements.txt's pin so tracking DB schema stays compatible).
mlflow==2.16.2
