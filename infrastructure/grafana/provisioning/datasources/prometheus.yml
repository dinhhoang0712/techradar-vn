apiVersion: 1

# Auto-provisioned Prometheus datasource so Grafana is query-ready on first boot.
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: prometheus
    url: http://prometheus:9090
    isDefault: false
