# API framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
sse-starlette==2.1.3
python-json-logger==2.0.7   # structured (JSON) logging — see app/observability.py

# Database
neo4j==5.19.0
SQLAlchemy==2.0.35
asyncpg==0.29.0
greenlet==3.1.1

# Config & validation
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1

# LangChain — chỉ còn dùng cho app/agent/executor.py (tool-calling agent cần LLM object
# kiểu LangChain). Đường generate()/generate_stream() chính (chat, RAG, các service khác)
# đã chuyển sang llm-gateway ở dưới.
langchain==0.2.16
langchain-core==0.2.43
langchain-google-genai==1.0.10
google-generativeai==0.7.2
langchain-openai==0.1.25
langchain-groq==0.1.9

# LLM Gateway — package dùng chung nhiều service (xem services/llm-gateway/): fallback
# giữa provider, rate limit theo token, tính cost. Cài kiểu editable từ path local trong
# repo — cùng dạng monorepo, không phải package publish lên PyPI.
# LƯU Ý: dòng -e path-tương-đối này hoạt động khi cài trực tiếp vào venv (pip install -r
# requirements.txt từ thư mục services/ai-rag-core/). Dockerfile hiện tại build context chỉ
# scope tới services/ai-rag-core/ nên KHÔNG thấy được services/llm-gateway/ — cần sửa build
# context + Dockerfile riêng trước khi build lại image Docker (chưa làm ở đây).
-e ../llm-gateway[all]

# Evaluation (RAGAS)
datasets==2.20.0
ragas==0.1.16
mlflow==3.12.0
tiktoken==0.7.0

# Embedding & reranking models
sentence-transformers==3.0.1
transformers==4.44.2
torch==2.4.1
onnxruntime==1.20.1
huggingface-hub==0.36.0

# Monitoring
prometheus-client>=0.20.0

# Numerical (forecast signals)
numpy>=1.26.0

# HTTP
httpx==0.27.2

# Test
pytest==8.4.2
pytest-asyncio==0.24.0
